# Changelog

## [0.1.0]

- Initial release of `vale-ears` package.
- Added `ears.Syntax` rule to enforce EARS templates on sentences containing `shall`.
- Added `ears.Shall` rule to suggest `shall` over `must`/`will`/`should`.
